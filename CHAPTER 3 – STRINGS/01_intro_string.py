name = "Meghna"

nameshort = name[0:3]  # Slicing the string to get the first three characters, only 0,1,2 excluding 3
print(nameshort)

character1=name[1] #writing just one letter using its postion
print(character1)